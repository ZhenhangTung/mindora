<div class="min-h-full">
  <%= render "layouts/app" %>

  <main class="-mt-32">
    <div class="mx-auto max-w-7xl px-4 pb-12 sm:px-6 lg:px-8">
      <%= render "layouts/career_menu" %>

      <ul role="list" class="divide-y divide-gray-100 overflow-hidden bg-white shadow-sm ring-1 ring-gray-900/5 sm:rounded-xl">
        <% @resumes.each do |resume| %>
          <li class="relative flex justify-between gap-x-6 px-4 py-5 hover:bg-gray-50 sm:px-6">
            <div class="flex min-w-0 gap-x-4">
              <!-- You might want to replace the img src with a relevant avatar if available -->
              <img class="h-12 w-12 flex-none rounded-full bg-gray-50" src="https://images.unsplash.com/photo-1494790108377-be9c29b29330?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=facearea&facepad=2&w=256&h=256&q=80" alt="">
              <div class="min-w-0 flex-auto">
                <p class="text-sm font-semibold leading-6 text-gray-900">
                  <a href="<%= resume_path(resume) %>">
                    <span class="absolute inset-x-0 -top-px bottom-0"></span>
                    <%= resume.name %>
                  </a>
                </p>
                <p class="mt-1 flex text-xs leading-5 text-gray-500">
                  <a href="mailto:<%= resume.email %>" class="relative truncate hover:underline"><%= resume.email %></a>
                </p>
              </div>
            </div>
            <div class="flex shrink-0 items-center gap-x-4">
              <div class="hidden sm:flex sm:flex-col sm:items-end">
                <!-- Assuming 'title' might be an attribute you want to display -->
                <!--          <p class="text-sm leading-6 text-gray-900"><%#= resume.title %></p>-->
                <p class="mt-1 text-xs leading-5 text-gray-500">最近更新时间 <time datetime="<%= resume.updated_at %>"><%= time_ago_in_words(resume.updated_at) %> ago</time></p>
              </div>
              <!-- Icon or action you might want to associate with each resume -->
              <svg class="h-5 w-5 flex-none text-gray-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                <path fill-rule="evenodd" d="M7.21 14.77a.75.75 0 01.02-1.06L11.168 10 7.23 6.29a.75.75 0 111.04-1.08l4.5 4.25a.75.75 0 010 1.08l-4.5 4.25a.75.75 0 01-1.06-.02z" clip-rule="evenodd" />
              </svg>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </main>
</div>



