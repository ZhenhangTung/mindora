class Assistant < ApplicationRecord
  # Validations can be added here, for example:

  # Allow external_id to be nil
  # validates :external_id, :name, :model, presence: true
  # validates :external_id, uniqueness: true


end
